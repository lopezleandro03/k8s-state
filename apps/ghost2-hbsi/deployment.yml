apiVersion: apps/v1
kind: Deployment
metadata:
  name: ghost2-hbsi
  namespace: ghost2-hbsi
  labels:
    app: ghost2-hbsi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ghost2-hbsi
  template:
    metadata:
      labels:
        app: ghost2-hbsi
    spec:
      containers:
      - name: ghost2-hbsi
        image: ghost
        env:
        - name: url
          value: https://ade-kaas-demo.westeurope.cloudapp.azure.com/ghost2-hbsi
        - name: NODE_ENV
          value: development
        resources:
          limits:
            cpu: "1"
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 64Mi
        ports:
        - name: http
          containerPort: 2368
          protocol: TCP
      restartPolicy: Always