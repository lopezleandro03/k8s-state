apiVersion: apps/v1
kind: Deployment
metadata:
  name: ghost-fyk
  namespace: ghost-fyk
  labels:
    app: ghost-fyk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ghost-fyk
  template:
    metadata:
      labels:
        app: ghost-fyk
    spec:
      containers:
      - name: ghost-fyk
        image: ghost
        env:
        - name: url
          value: https://ade-kaas-demo.westeurope.cloudapp.azure.com/ghost-fyk
        - name: NODE_ENV
          value: development
        resources:
          limits:
            cpu: "1"
            memory: 256Mi
          requests:
            cpu: 100m
            memory: 64Mi
        ports:
        - name: http
          containerPort: 2368
          protocol: TCP
      restartPolicy: Always