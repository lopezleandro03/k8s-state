apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ghost-ht6
  namespace: ghost-ht6
spec:
  accessModes:
    - ReadWriteMany
  # persistentVolumeReclaimPolicy: Retain
  storageClassName: azurefile
  resources:
    requests:
      storage: 50Gi