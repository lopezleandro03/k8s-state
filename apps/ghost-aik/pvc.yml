apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ghost-aik
  namespace: ghost-aik
spec:
  accessModes:
    - ReadWriteMany
  # persistentVolumeReclaimPolicy: Retain
  storageClassName: azurefile
  resources:
    requests:
      storage: 50Gi